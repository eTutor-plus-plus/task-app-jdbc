version: '3'

services:
  postgres:
    image: postgres:16
    environment:
      - 'POSTGRES_DB=etutor_JDBC'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=etutor_JDBC'
    ports:
      - '5434:5432'
